ADRESSE PUBLIQUE AFRICHAIN (sandbox)
================================

  https://basement-ending-messages-health.trycloudflare.com

(Le sandbox dort la nuit — l'adresse peut mourir.
Le telephone du Chef a son propre tunnel dans son Termux :
  cloudflared tunnel --url http://localhost:8080
Si l adresse ne marche plus :
  cd ~/afririch && git pull && cat adresse.txt)
